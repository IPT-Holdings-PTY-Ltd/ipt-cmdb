name: msp-cmdb-hub
services:
  api:
    project: .
    host: containerapp
    language: python
    docker:
      path: ./Dockerfile
      context: .
      platform: amd64
      remoteBuild: true
infra:
  provider: bicep
  path: infra
  module: main
